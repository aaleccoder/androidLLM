/**
 * Main Entry Point
 * 
 * This file serves as the initial entry point for the app,
 * following Expo guidelines for clean routing.
 */
import { Login } from '../components/Login';

export default function App() {
  return <Login />;
}
